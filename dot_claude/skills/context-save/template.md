---
project: リポジトリ名
git_remote: https://github.com/user/repo.git
branch: feature/xxx
updated: YYYY-MM-DDTHH:mm:ss
tags:
  - claude-context
---

## プロジェクト概要

（プロジェクトの簡潔な説明。既存があれば引き継ぐ）

## 現在の状態

- **ブランチ**: `feature/xxx`
- **直近のコミット**:
  - `abc1234` コミットメッセージ1
  - `def5678` コミットメッセージ2
- **未コミットの変更**: あり/なし（概要）

## 進行中の作業

各 entry は `- YYYY-MM-DD（任意の補足）: 内容` 形式で書く（日付プレフィックスがローテーションの基準。14 日より古い entry は次回 save 時に自動削除されるため、ここに書く内容は時系列ログとして扱う）。

- YYYY-MM-DD: 現在取り組んでいるタスクや作業の説明
- YYYY-MM-DD（午後・完了）: 未完了の作業や完了状態の補足があればここに記載

## 判断メモ

- セッション中に行った重要な判断とその理由
- 「なぜそうしたか」が後からわかるように記録
- （判断がなければこのセクションごと省略）

## 関連リポジトリ

（ホームワークスペース等、CWD の直接 git 管轄外だが関連するリポジトリを記録する。不要なら省略）

- **dotfiles (chezmoi)**: `git@host:user/repo.git`
  - source path: `/path/to/chezmoi/source`
  - 直近のコミット:
    - `abc1234` メッセージ1
    - `def5678` メッセージ2

## 重要ファイル

| ファイル | 役割 |
|---|---|
| `path/to/file` | 説明 |

## メモ

（その他、次回セッションで知っておくべきこと。なければ省略）
