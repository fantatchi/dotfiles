{{- if .use_github_mcp }}
{
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": {{ .github_token | quote }}
      }
    }
  }
}
{{- else }}
{
  "mcpServers": {}
}
{{- end }}
